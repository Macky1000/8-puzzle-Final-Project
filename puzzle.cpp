/*#include "puzzle.h"

void readInput(const string& defaultInput, int& matrix){
    //ifstream
    ifstream in(defaultInput);
    if (!in.is_open()){
        cout << "Couldn't open " << defaultInput << " for reading" << endl;
    }
}*/